# Agents.md — Руководство для ИИ-агента по эмулятору БЭСМ-6 (SIMH)

## 1. Общая цель
Добавить и отладить поддержку терминалов, подключённых через устройство КАДОПАМ‑КРК (ДКС) в эмуляторе БЭСМ‑6.

**Связанный проект:** `~/Yandex.Disk/re_dispak/re-dispak` — анализ ОС Диспак, модуль СВЯЗЬ7 (зона 0677).
См. также `TEAM_AGENTS.md` для координации агентов.

## 2. Архитектура эмулятора
- Эмулятор написан на C, базируется на SIMH.
- Моделирует процессор БЭСМ‑6, память, внешние устройства (диски, ленты, АЦПУ, терминалы).
- Терминалы представлены массивом `tty_unit[27]`:
  - `[0]` — тактовый таймер;
  - `[1..24]` — последовательные линии (ТТ или ВТ‑340);
  - `[25..26]` — параллельные «Консулы».
- Каждая линия имеет флаги (`tty_unit[num].flags`), определяющие режим работы:
  - `TTY_OFFLINE_STATE`, `TTY_TELETYPE_STATE`, `TTY_VT340_STATE`, `TTY_CONSUL_STATE`;
  - `TTY_MUX_MODE` — линия работает через мультиплексор (АС);
  - `TTY_DKS_MODE` — линия работает через ДКС (КАДОПАМ‑КРК).

**Навыки:**
- `GENS_AGENTS.md` — работа с генератором системы ГЕНС и сменой конфигураций

## 3. Модель терминалов
Эмулятор **не читает** конфигурацию ОС Диспак. Всё задаётся пользователем через команды эмулятора:
- `SET ttyN [tt|vt|consul|off]` — тип линии;
- `SET ttyN [mux|dks]` — режим подключения;
- `ATTACH ttyN <порт>` — сетевое подключение;
- `ATTACH ttyN console` — локальная консоль.

Активные линии собираются в маски `tt_mask` и `vt_mask`. Для MUX и DKS эти маски не используются: обработка идёт через отдельные механизмы (`mux_*` и `dks_*`).

## 4. Поддержка ДКС (КАДОПАМ‑КРК)
### 4.1. Память КАДОПАМ
Эмулятор содержит массив `kadopam_mem[65536]` — образ 16‑разрядной памяти микро‑ЭВМ «Электроника‑60».  
Доступ к нему из БЭСМ‑6 осуществляется командами **032** (чтение) и **132** (запись).  
Обработка этих команд находится в `besm6_cpu.c`: функции `read_032()` и `write_032()`.

### 4.2. Прерывания
При работе ДКС используются прерывания ПРП (периферийный регистр прерываний):
- **ПРП5** — внимание / быстрый обмен;
- **ПРП6** — готовность приёма (медленный обмен);
- **ПРП7** — запрос ввода H‑терминала;
- **ПРП8** — готовность к передаче (быстрый обмен);
- **ПРП12** — запрос ввода S‑терминала / системный запрос.

Каждому событию соответствует константа в `besm6_cpu.c`:
- `KADOPAM_EVENT_SREQ` (ПРП12),
- `KADOPAM_EVENT_XMIT` (ПРП8),
- `KADOPAM_EVENT_ATTN` (ПРП5),
- `KADOPAM_EVENT_RECV` (ПРП6),
- `KADOPAM_EVENT_TERMREQ` (ПРП7).

Функция `kadopam_svc()` вызывается по таймеру и устанавливает биты в `PRP` и `GRP`.  
Внешнее прерывание процессора инициируется в `sim_instr()` при условии `(GRP & MGRP)` и разрешённых прерываниях в `M[PSW]`.

### 4.3. Подключение терминала через ДКС
Алгоритм (должен быть реализован в `besm6_tty.c`):

1. При новом telnet‑подключении, если линия в режиме `TTY_DKS_MODE`, вызывается `dks_register(num, line)`.
2. `dks_register`:
   - выделяет структуру `dks_term`;
   - назначает базовый адрес буфера в `kadopam_mem` (начиная с `0x2000`, шаг `0x100`);
   - записывает команду подключения в системный буфер S‑машины:
     ```c
     kadopam_mem[000154] = 1;          // команда 1 – подключение
     kadopam_mem[000155] = unit;       // логический номер терминала
     kadopam_mem[000156] = base_addr;  // адрес буфера S‑терминала
генерирует прерывание ПРП12 (KADOPAM_EVENT_SREQ).

ОС Диспак по прерыванию ПРП12 читает системный буфер, выполняет процедуру нтерм7 и подключает терминал.

4.4. Приём символа
При нажатии клавиши на telnet‑клиенте:

dks_poll() опрашивает линию, получает символ через tmxr_getc_ln().

Символ записывается в kadopam_mem[base_addr] (16‑бит).

Генерируется прерывание ПРП7 (KADOPAM_EVENT_TERMREQ).

ОС по ПРП7 читает данные из буфера S‑терминала и передаёт в МОТТ.

5. Текущее состояние и известные проблемы
Эмулятор генерирует прерывания, но ОС не реагирует (нет сообщений о чтении/записи из КАДОПАМ).

По логу видно, что внешние прерывания возникают, но в GRP часто присутствует только бит таймера, а не ПРП.

Причина, вероятно, в том, что ОС не инициализировала ДКС (модуль СВЯЗЬ7 не загружен) или маски MGRP/MPRP не установлены.

В эмуляторе уже добавлены принудительные маски в cpu_reset, но они могут быть сброшены ОС.

6. Задачи для агента
Проверить обработку команд 032/132:

Адресация должна быть 15‑битной (addr &= 077777).

Запись: извлекать 16 бит из разрядов 24–39 сумматора ((val >> 24) & 0xFFFF).

Чтение: возвращать значение, сдвинутое влево на 24, и объединять с младшими 24 битами сумматора.

Проверить dks_register:

Обязательно записывать команду подключения в kadopam_mem[000154..156].

Генерировать KADOPAM_EVENT_SREQ (ПРП12), а не XMIT.

Проверить dks_poll:

Генерировать KADOPAM_EVENT_TERMREQ (ПРП7) при поступлении символа.

Убедиться, что маски устанавливаются:

В cmd_033 при записи MPRP автоматически устанавливать бит 37 в MGRP.

В kadopam_svc для всех событий добавлять MGRP |= GRP_SLAVE; (если ещё не сделано).

Проверить сброс PRP:

Гашение PRP должно выполняться по команде УВВ 030 с правильной маской (PRP &= ACC | PRP_WIRED_BITS;), не инвертировать.

Проверить инициализацию ДКС в ОС:

Убедиться, что модуль СВЯЗЬ7 присутствует в образе диска 2053 (зона 0677) и загружается при старте.

Если модуль отсутствует, для теста можно временно эмулировать подмену векторов прерываний ПРП 5–8,12 на заглушки, которые сбрасывают соответствующий бит PRP и вызывают обработчик.

7. Правила и ограничения
Не использовать фиксированные адреса ОС (например, ШКУТ) в эмуляторе.

Не менять логику команд 032/132 без явной необходимости.

Не откатывать исправление гашения ПРП.

Все изменения должны быть минимальны и соответствовать документации на БЭСМ‑6 и ОС Диспак.

Сборка: make. Запуск: ./BIN/besm6 dispak.ini.

Лог отладки: debug.txt (включается set console debug=debug.txt).

8. Полезные команды для проверки
bash
# Включить отладку в dispak.ini
set console debug=debug.txt

# Запустить эмулятор
./BIN/besm6 dispak.ini

# Просмотреть лог
tail -f debug.txt

## 9. Связь с проектом re-dispak
- **Модуль СВЯЗЬ7**: зона 0677 диска 2053
- **Документация ДКС**: `~/Yandex.Disk/re_dispak/re-dispak/RUKDKS`
- **Трассы ОС**: анализировать при проблемах с реакцией ОС
- **Скрипты**: `asm.pl`, `trace.pl` — для проверки модулей

При отсутствии реакции ОС — проверить загруженность СВЯЗЬ7 через re-dispak.

## 10. Критерии успеха
При подключении telnet к линии с режимом DKS в логе появляются:

DKS terminal registered: unit=N, base=0xXXXX

+++ KDP: генерация прерывания ПРП12 (SREQ)

ОС Диспак отвечает на подключение (в логе появляются записи в буфер H‑машины, приглашение на терминале).

При нажатии клавиш на telnet ОС обрабатывает символы (появляется эхо или выполняется команда).

Нет бесконечного потока одинаковых прерываний (после сброса PRP обработчиком прерывание гаснет).

## 11. Конфигуратор системы ГЕНС

### 11.1. Общие сведения
ГЕНС — генератор системы ОС Диспак. Создаёт конфигурацию системы на основе входного файла `.b6`.
Варианты конфигураций находятся в связанном проекте: `~/Yandex.Disk/re_dispak/re-dispak/gens/`

**Форматы файлов:**
- `.b6` — входной файл конфигурации для ГЕНС (формат БЭСМ-6)
- Файлы `1`–`9` — примеры конфигураций с входной информацией и результатом работы ГЕНС

### 11.2. Структура файла конфигурации (.b6)

**Заголовок файла:**
```
ШИФР <код>‾ОЗУ <объём>‾ВХО <количество_входов>‾ВРЕ <время>‾РОС <режим>‾Е
B63
K 07 010 0074 00 050 0131      # Команды загрузки
K 07 26 00065 00 074 0000
...
C 0010 0100 0030 0752          # Константы
...
B100A1                        # Начало описания конфигурации
```

**Директивы конфигурации (после B100A1):**

| Директива | Описание | Пример |
|-----------|----------|--------|
| `МЛЗАГ` | Магнитная лента загрузчика | `МЛЗАГ 2053-560^` |
| `МЛСТА` | МЛ старта | `МЛСТА 2053^` |
| `МЛВВО` | МЛ ввода | `МЛВВО 2053^` |
| `МЛВЫВ` | МЛ вывода | `МЛВЫВ 2053^` |
| `СЛОЙК` | Слоистая конфигурация | `СЛОЙК ДА^` |
| `МБКОН` | Модуль констант | `МБКОН МБ20Т33^` |
| `МБИНФ` | Модуль информации | `МБИНФ 21^` |
| `МБПРО` | Модуль программ | `МБПРО 22^` |
| `ОЗУ` | Объём оперативной памяти | `ОЗУ 512K^` |
| `РЕСУР` | Ресурсы системы | `РЕСУР 371,400^` |
| `НОММА` | Номер машины | `НОММА 3^` |
| `ГОД` | Текущий год | `ГОД 2024^` |
| `ДИСКО` | Дисковое пространство | `ДИСКО 2999^` |
| `ТРГЕН` | Триггеры генерации | `ТРГЕН МБ20Т34^` |
| `УКВП` | Управляющие каналы | `УКВП Л0:0-7,Л1:0-7,...^` |
| `НРДИС` | Номер диска | `НРДИС 2053,27,01^` |
| `ТКАНА` | Тип каналами | `ТКАНА Ф1:0-7^` |
| `ШКСВМ` | Шкаф СВМ | `ШКСВМ А0Л5-А3Л7^` |
| `УСТПЛ` | Устройство печати | `УСТПЛ ПЛ-80^` |
| `СЭВМ` | Совместимость с ЭВМ | `СЭВМ ДА^` |
| `АРХИВ` | Архивация | `АРХИВ ДА^` |
| `ШКОПТ` | Шкаф опций | `ШКОПТ Ф1^` |
| `ЛИСТА` | Листание | `ЛИСТА 6^` |
| `Э256К` | Расширение памяти 256К | `Э256К ДА^` |

### 11.3. Конфигурация терминалов

**Ключевые директивы для терминалов:**

| Директива | Описание | Пример |
|-----------|----------|--------|
| `ТТ` | Номер телетайпа (главный терминал) | `ТТ 1^` |
| `ТЕРМ` | Подключённые терминалы | `ТЕРМ VT:1-10^` |
| `ТЕРМ` | Терминалы через АС (МУКС) | `ТЕРМ АС,ТТ:11-16^` |
| `ТЕРМ` | Терминалы через АС (VT) | `ТЕРМ АС,VT:3-47^` |
| `ЕСТЕР` | Дополнительные терминалы | `ЕСТЕР 1-35^` |
| `ЧАСЫ` | Тип часов | `ЧАСЫ ИПМ^` |

**Форматы описания терминалов:**

1. **Локальные терминалы (VT/ТТ):**
   - `ТЕРМ VT:1-10` — терминалы ВТ-340, линии 1-10
   - `ТЕРМ TT:1-16` — телетайпы, линии 1-16

2. **Терминалы через АС (МУКС — мультиплексор):**
   - `ТЕРМ АС,ТТ:11-16` — телетайпы через АС, линии 11-16
   - `ТЕРМ АС,VT:3-47` — ВТ-340 через АС, линии 3-47

3. **Терминалы через ДКС (КАДОПАМ-КРК):**
   > В конфигурациях ГЕНС ДКС-терминалы обычно не указываются явно.
   > Для ДКС требуется наличие модуля СВЯЗЬ7 (зона 0677).
   > Настройка ДКС в эмуляторе выполняется командами:
   ```
   SET ttyN vt          # тип терминала
   SET ttyN dks         # режим ДКС
   ATTACH ttyN <порт>   # подключение
   ```

### 11.4. Примеры конфигураций из gens/

**Файл `gens.b6` (базовая конфигурация):**
- 512К ОЗУ
- Диски 2053
- VT терминалы 1-10
- АС терминалы 11-16 (ТТ)
- УКВП на всех линиях (Л0-Л7)

**Файл `gens_exp.b6` (экспериментальная конфигурация):**
- ТТ:1
- VT:1-2 (локальные)
- АС,VT:3-16 (через мультиплексор)
- ТТ:11-16

**Файл `gens_rab.b6` (рабочая конфигурация):**
- VT:1-30 (много терминалов)
- АС,ТТ:3-10
- ТТ:11-16

**Файл `3` (расширенная конфигурация):**
- VT:1-2
- АС,VT:3-47 (до 47 терминалов через АС)
- ЕСТЕР 1-35
- ЧАСЫ ИПМ

### 11.5. Секция "ИНФОРМАЦИЯ К ГЕНСУ"

После описания конфигурации идёт секция с 16-ричными данными (адреса 72000-72030):
```
ИНФОРМАЦИЯ К ГЕНСУ

72000
 0000056000004005    # Конфигурационное слово 0
 0000000000004005    # Конфигурационное слово 1
 ...
72010
 0000000000000022    # Маски прерываний и т.д.
 ...
```

Эти данные формируются ГЕНС на основе входной конфигурации.

### 11.6. Связь конфигурации ГЕНС с эмулятором

**Важно:** Эмулятор НЕ читает файлы конфигурации ГЕНС.

Соответствие между конфигурацией ГЕНС и настройками эмулятора:

| ГЕНС | Эмулятор (команда) |
|------|-------------------|
| `ТТ 1` | `SET tty1 tt` |
| `ТЕРМ VT:1-10` | `SET ttyN vt` для N=1..10 |
| `ТЕРМ АС,ТТ:11-16` | `SET ttyN tt`, `SET ttyN mux` для N=11..16 |
| ДКС (СВЯЗЬ7) | `SET ttyN dks` для нужных линий |

**Пример настройки эмулятора под конфигурацию:**
```ini
# Конфигурация терминалов для gens_rab.b6
# VT:1-30
SET tty1 vt
ATTACH tty1 4001
...
SET tty30 vt
ATTACH tty30 4030

# АС,ТТ:3-10
SET tty3 tt
SET tty3 mux
...
```

### 11.7. Рекомендации по работе с конфигурациями

1. **Выбор конфигурации:**
   - Для базового тестирования: `gens.b6` или `gens_test.b6`
   - Для работы с множеством терминалов: `gens_rab.b6`
   - Для ДКС: использовать конфигурацию с СВЯЗЬ7 (зона 0677)

2. **Проверка конфигурации:**
   - Убедиться, что директивы корректны (оканчиваются на `^`)
   - Проверить соответствие номеров терминалов линиям эмулятора
   - Проверить наличие необходимых модулей (СВЯЗЬ7 для ДКС)

3. **Создание тестовой конфигурации:**
   - Скопировать существующий `.b6` файл
   - Изменить параметры терминалов
   - Запустить ГЕНС на ОС Диспак для генерации новой системы

4. **Отладка проблем с терминалами:**
   - Проверить соответствие линии в ГЕНС и эмуляторе
   - Убедиться, что модуль СВЯЗЬ7 загружен (для ДКС)
   - Проверить маски прерываний в секции 72010

---

## 12. Оптимизация работы Cline

### 12.1. Правила экономии токенов

Проблема: в сессии отладки ДКС было потеряно ~20,000 токенов (30%) из-за:
- Таймаутов при многократной сборке
- Повторного чтения одних файлов
- Дублирования grep-запросов
- Лишних диагностических команд

#### Правило 1: Пакетные операции

**Всегда группировать независимые операции в один вызов.**

```
Плохо:
  read_files: [besm6_cpu.c]
  read_files: [besm6_tty.c]
  read_files: [dispak.ini]

Хорошо:
  read_files: [besm6_cpu.c, besm6_tty.c, dispak.ini]
```

Экономия: ~3,000 токенов на задачу.

#### Правило 2: Фоновые команды

**Долгие команды (make, gcc, npm test) всегда в фоне.**

```
Плохо:
  run_commands: ["make"]  # Таймаут!

Хорошо:
  run_commands: ["make > /tmp/build.log 2>&1 &"]
  run_commands: ["sleep 3; tail -20 /tmp/build.log"]
```

Экономия: ~5,000 токенов на таймаут.

#### Правило 3: Кэширование поиска

**Сохранять результаты grep во временные файлы.**

```
Плохо:
  grep -rn "TMXR_VALID" *.c  # Первый раз
  grep -rn "TMXR_VALID" *.c  # Повтор!

Хорошо:
  grep -rn "TMXR_VALID" *.c > /tmp/search_tmxr.txt
  cat /tmp/search_tmxr.txt  # При необходимости
```

Экономия: ~1,500 токенов на повтор.

#### Правило 4: Ограничение вывода

**Использовать head/tail для больших файлов.**

```
Плохо:
  cat debug.txt  # Весь файл

Хорошо:
  tail -50 debug.txt
```

Экономия: ~500 токенов на вызов.

#### Правило 5: Убрать декорации

**Не использовать echo для разделителей.**

```
Плохо:
  echo "=== Начало секции ==="
  echo "--- Конец ---"

Хорошо:
  # Просто выполнить команды без декораций
```

Экономия: ~2,000 токенов на сессию.

### 12.2. Скрипты-помощники

Для типовых операций созданы скрипты в `scripts/`:

| Скрипт | Назначение | Использование |
|--------|------------|---------------|
| `build.sh` | Фоновая сборка | `./scripts/build.sh` |
| `test_dks.sh` | Запуск теста ДКС | `./scripts/test_dks.sh` |
| `stop_emulator.sh` | Остановка эмулятора | `./scripts/stop_emulator.sh` |
| `monitor_dks.sh` | Мониторинг логов | `./scripts/monitor_dks.sh` |
| `search_code.sh` | Кэшированный поиск | `./scripts/search_code.sh pattern` |

### 12.3. Чек-лист перед каждым вызовом

Перед вызовом инструмента проверить:

- [ ] **Файл уже читался?** → Использовать кэш, не читать снова
- [ ] **Команда независима?** → Группировать с другими
- [ ] **Команда долгая (>5 сек)?** → Запустить в фоне
- [ ] **Вывод большой (>50 строк)?** → Использовать head/tail
- [ ] **Поиск повторяется?** → Проверить /tmp/search_*.txt
- [ ] **Нужен ли этот echo?** → Убрать декорации

### 12.4. Пример оптимизированного рабочего процесса

**Задача:** Найти и исправить ошибку в dks_poll()

```
НЕОПТИМИЗИРОВАНО (старый подход):
  1. read_files: [besm6_tty.c]           → ~5,000 токенов
  2. grep "dks_poll" besm6_tty.c         → ~500 токенов
  3. read_files: [besm6_tty.c] (повтор)  → ~5,000 токенов
  4. edit besm6_tty.c                    → ~1,000 токенов
  5. make                                → ~3,000 токенов (таймаут!)
  6. make (повтор)                       → ~3,000 токенов
  ИТОГО: ~17,500 токенов

ОПТИМИЗИРОВАНО:
  1. grep -n "dks_poll" besm6_tty.c > /tmp/dks.txt  → ~500 токенов
  2. read_files: [{besm6_tty.c, lines 270-290}]      → ~500 токенов
  3. edit besm6_tty.c                                 → ~1,000 токенов
  4. make > /tmp/build.log 2>&1 &                    → ~100 токенов
  5. tail /tmp/build.log                              → ~200 токенов
  ИТОГО: ~2,300 токенов

ЭКОНОМИЯ: 87%
```

### 12.5. Метрики эффективности

| Метрика | Было | Цель | Как измерить |
|---------|------|------|--------------|
| Токены на задачу | 70,000 | 45,000 | Счётчик Cline |
| Повторные чтения | 3-4 | 1 | Анализ логов |
| Таймауты | 5-6 | 0-1 | Ошибки timeout |
| Дублирование поиска | 2-3 | 0 | grep истории |
| Декоративные команды | 20+ | 5 | echo/ls/file |

### 12.6. Конфигурационный файл .clinerules

Подробные правила оптимизации находятся в `.clinerules` в корне проекта.

**Важно:** Cline автоматически читает этот файл при старте сессии.

## 13. Статус ДКС и связанные модули ОС (обновлено 2025-08-27)

### 13.1. Текущий статус поддержки ДКС

- [x] Команды 032/132 реализованы в besm6_cpu.c
- [x] Прерывания ПРП5-8,12 генерируются в kadopam_svc()
- [x] Функции dks_register(), dks_poll() реализованы в besm6_tty.c
- [ ] Реакция ОС на прерывания — требует отладки

### 13.2. Связанные модули ОС Диспак

| Модуль  | Зона      | Назначение                    | Статус        |
|---------|-----------|-------------------------------|---------------|
| СВЯЗЬ7  | 0677      | Обработка прерываний ДКС      | Требует анализа |
| УКАЧКА  | 010400(8) | Откачка задач, обмен с ВЗУ   | ✓ Прокомментирован |

### 13.3. Взаимодействие УКАЧКА с ДКС

Модуль УКАЧКА использует ДКС для обмена данными:
- Команда **032** — чтение из kadopam_mem (буферы терминалов)
- Команда **132** — запись в kadopam_mem (передача данных на ВЗУ)
- Прерывания ПРП5-8,12 — сигналы готовности обмена

### 13.4. Рекомендации по отладке

1. Проверить загруженность модуля СВЯЗЬ7 в зону 0677
2. Проверить маски MGRP/MPRP при работе ДКС
3. Сравнить трассы ОС с работой модуля УКАЧКА
4. Использовать `debug.txt` для мониторинга прерываний

## 14. Справочник процедур модулей ОС Диспак

### 14.1 Модуль УКАЧКА (зона 010400)

| Процедура | Назначение | Ключевые операции |
|-----------|------------|-------------------|
| ТП1 | Формирование адреса обмена | Вычисление адреса буфера в МО |
| ТП2 | Поиск свободного тракта | Сканирование шкалы занятости |
| ТП3 | Резервирование тракта | Установка бита в шкале |
| ТП4 | Обновление шкалы НМБ | Сброс/установка битов занятости |
| ТП5 | Переключение страниц ТБД | Выбор активной страницы |
| ТП7 | Связывание трактов | Построение цепочки буферов |
| ПТА | Поиск тракта для откачки | Выбор по приоритету |
| ЕСП | Запросы подкачки | Обработка очереди запросов |
| ПОМБ | Обмен с МБ | Чтение/запись НМБ |
| ПОБМД | Обмен с МД | Чтение/запись РМД |

### 14.2 Модуль СВЯЗЬ7 (зона 0677) — ДКС

| Процедура | Назначение | Прерывание |
|-----------|------------|------------|
| НТЕРМ7 | Подключение терминала | Инициализация |
| ПРП5 | Внимание/быстрый обмен | KADOPAM_EVENT_ATTN |
| ПРП6 | Готовность приёма | KADOPAM_EVENT_RECV |
| ПРП7 | Запрос ввода H-терминала | KADOPAM_EVENT_TERMREQ |
| ПРП8 | Готовность передачи | KADOPAM_EVENT_XMIT |
| ПРП12 | Запрос S-терминала | KADOPAM_EVENT_SREQ |

### 14.3 Карта памяти ДКС (kadopam_mem)

| Адрес | Назначение | Доступ |
|-------|------------|--------|
| 000154 | Команда подключения | Эмулятор → ОС |
| 000155 | Номер терминала | Эмулятор → ОС |
| 000156 | Адрес буфера S-терминала | Эмулятор → ОС |
| 002000+ | Буферы S-терминалов | ОЗУ ДКС |
| 002100+ | Буферы H-терминалов | ОЗУ ДКС |

**Шаг буфера:** 0x100 (256 слов) на терминал

### 14.4 Соответствие констант эмулятора и ОС

| Константа в besm6_cpu.c | Прерывание | Обработчик в ОС |
|-------------------------|------------|-----------------|
| KADOPAM_EVENT_SREQ | ПРП12 | СВЯЗЬ7.ПРП12 |
| KADOPAM_EVENT_XMIT | ПРП8 | СВЯЗЬ7.ПРП8 |
| KADOPAM_EVENT_ATTN | ПРП5 | СВЯЗЬ7.ПРП5 |
| KADOPAM_EVENT_RECV | ПРП6 | СВЯЗЬ7.ПРП6 |
| KADOPAM_EVENT_TERMREQ | ПРП7 | СВЯЗЬ7.ПРП7 |

## 15. Управление конфигурационными файлами в git

### 15.1 Правило

При создании или изменении конфигурационных файлов агента они должны быть добавлены в индекс командой `git add` сразу после изменения.  
**Коммит делается ТОЛЬКО по команде РЗВ** (резервная копия).

### 15.2 Обязательные файлы

| Файл            | Назначение                      |
|-----------------|---------------------------------|
| .clinerules     | Правила проекта                 |
| AGENTS.md       | Инструкции для агента           |
| TEAM_AGENTS.md  | Координация агентов             |
| GENS_AGENTS.md  | Работа с генератором системы    |

### 15.3 Команды

```bash
# Добавление (выполнять сразу после изменения)
git add .clinerules AGENTS.md TEAM_AGENTS.md GENS_AGENTS.md

# Коммит (ТОЛЬКО по команде РЗВ)
git commit -m "Обновлены конфигурационные файлы"
```

### 15.4 Исключения (НЕ добавлять)

- `*.bak`, `*.bak.*` — резервные копии
- `*.swp`, `*.swo` — файлы vim
- `cline_config_*.tar.gz` — архивы бэкапов
- `debug.txt`, `log.txt` — временные логи
- `*.o`, `*.rez` — объектные файлы

